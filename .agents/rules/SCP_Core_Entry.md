# SCP_Core 共用規則（Antigravity）

> 本檔由 SCP_Core 的 skill 管理頁**整檔產生** —— 手改會在下次同步被覆蓋。
> 專案自己的規則請寫在專案的規則檔，不要寫在這裡。

跨專案的 agent 機制（指令系統 / 後台頁 / 記憶與信件層）由 `SCP_Core` 集中管理。
完整入口見 [`SCP_Core/AgentEntry/SCP_Core_Entry.md`](SCP_Core/AgentEntry/SCP_Core_Entry.md)。
