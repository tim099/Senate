### Windows
# Windows thumbnail cache files
Thumbs.db

# Folder config file
[Dd]esktop.ini

# Recycle Bin used on file shares
$RECYCLE.BIN/

# Windows shortcuts
*.lnk

### .NET build 產物（全部是 restore/build 生成、且帶機器絕對路徑，一律不入版控）
# 🩸 obj/project.assets.json 與 *.nuget.g.props 裡有
#    packageFolders = C:\Users\<你>\.nuget\packages\ —— 那是**這台機器**的 NuGet 快取位置。
#    進了版控＝下一台機器 clone 到一份指著不存在路徑的檔，而 restore 每次又會覆寫它，
#    於是每個人每次 commit 都帶一筆假 diff。它是產物，不是設定。
bin/
obj/
build/
publish/
*.user
*.suo
.vs/

### Senate 本機設定（含絕對路徑 ⇒ 一台機器一份）
# 樣板在 config/senate.local.example.json（那份入版控、不含任何機器路徑）
senate.local.json

### ImGui 視窗佈局（使用者自己拖出來的，屬於這台機器的偏好）
imgui.ini

### 一鍵 build 的產物（repo 根的執行檔）
/senate.exe

### 一鍵 build 的產物
/senate.cmd
/senate

### 一鍵 build 的產物（根層執行檔與原生 DLL）
/senate.exe
/cimgui.dll
/glfw3.dll
