### Windows
# Windows thumbnail cache files
Thumbs.db

# Folder config file
[Dd]esktop.ini

# Recycle Bin used on file shares
$RECYCLE.BIN/

# Windows shortcuts
*.lnk

### .NET build 產物（全部是 restore/build 生成、且帶機器絕對路徑，一律不入版控）
# 🩸 obj/project.assets.json 與 *.nuget.g.props 裡有
#    packageFolders = C:\Users\<你>\.nuget\packages\ —— 那是**這台機器**的 NuGet 快取位置。
#    進了版控＝下一台機器 clone 到一份指著不存在路徑的檔，而 restore 每次又會覆寫它，
#    於是每個人每次 commit 都帶一筆假 diff。它是產物，不是設定。
bin/
obj/
build/
publish/
*.user
*.suo
.vs/

### SenateData/ —— Senate 自己的資料根（版面見 Docs/Architecture/Data_Layout.md）
# 判準是「這個檔掉了，使用者要不要重做工？」：
#   config/  人編輯的設定  ⇒ 只有樣板入版控，本機那份不入
#   prefs/   程式替使用者寫的偏好（頁面設定 / ImGui 版面）⇒ 全不入
#   runtime/ 進程活著才有意義的狀態 ⇒ 全不入，且應該被清
# ⚠ 用「先全擋、再放行樣板」而不是逐檔列舉：
#   逐檔列舉的話，日後新增一個帶機器路徑的檔會**預設入版控**，
#   而那件事不會有人發現 —— 它長得就像一筆正常的 diff。
/SenateData/
!/SenateData/config/
!/SenateData/config/senate.local.example.json

### 一鍵 build 的產物 —— 根層現在只剩一個給人雙擊的捷徑（執行檔住 publish/，已被上面擋掉）
/senate.lnk

### 一鍵 build 的產物
/senate.cmd
/senate


